<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betav">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Areas"></activity>
        <activity android:name=".Categories" />
        <activity android:name=".Profile" />
        <activity android:name=".History" />
        <activity android:name=".Permits" />
        <activity android:name=".complaintPage" />
        <activity android:name=".MainStuRespo" />
        <activity android:name=".createComplain" />
        <activity android:name=".SignIn" />
        <activity android:name=".mainMaintenance" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>